#!/bin/bash
# Enforces layer boundaries (eslint) and API route shape (check-pattern) on
# every commit. Tests and type-checking run in CI, not here, so the hook
# stays fast. See PATTERN.md.
cd "$(git rev-parse --show-toplevel)"
npm run lint && npm run check:pattern
